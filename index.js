const franc = require('franc');
const langs = require('langs');

const sentence = process.argv.slice(2);

const langCode = franc("I'm going to the chuch tomorrow");

const langObj = langs.where("3", codigo);
console.log(langObj.name);